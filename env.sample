# Copy to .env.local and fill values
# Public site URL for sitemap/robots
NEXT_PUBLIC_SITE_URL=

# Resend email settings (preferred)
RESEND_API_KEY=
RESEND_FROM=
RESEND_TO=

# Generic contact recipient (used by the app). Set this to your email address
# if you want messages delivered to your inbox.
CONTACT_TO=nautiyalb8@gmail.com

# Optional SMTP settings: if you provide these the app will send emails via SMTP
# directly (using Nodemailer). Useful if you have an SMTP server or SMTP service
# (e.g., Gmail with app password, Mailgun SMTP, etc.).
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# If using EmailJS instead of Resend (optional, not yet wired)
EMAILJS_SERVICE_ID=
EMAILJS_TEMPLATE_ID=
EMAILJS_PUBLIC_KEY=
